.ui.grid
	.wide.column.navbar(ng-include="'components/navbar/navbar.html'" ng-class="{three: !collapsed, one: collapsed}")
	.wide.column.mainview(ng-class="{thirteen:!collapsed, fifteen:collapsed}")
		//- This is a hack for routing. UI-router re-renders entire view on state change. Causes 1 second + lag and doesn't maintain state.
		//- Using ng-show prevents re-render of DOM, improving UI responsiveness by trading memory.
		include ../champions/champions.jade
		include ../backpacks/backpacks.jade
		include ../build/build.jade